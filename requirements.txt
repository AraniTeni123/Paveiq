# Geospatial core
geopandas>=0.14.0
shapely>=2.0.0
pyproj>=3.6.0
pyogrio>=0.7.0

# Raster and imagery
rasterio>=1.3.0
rioxarray>=0.15.0

# Numeric / data
numpy>=1.24.0
pandas>=2.0.0
pyarrow>=14.0.0

# Machine learning
scikit-learn>=1.3.0
xgboost>=2.0.0
lightgbm>=4.1.0

# Visualization (notebooks)
matplotlib>=3.7.0
seaborn>=0.13.0
folium>=0.15.0
contextily>=1.4.0

# Utilities
tqdm>=4.66.0
pyyaml>=6.0
python-dotenv>=1.0.0
pydantic>=2.5.0

# Notebook
jupyter>=1.0.0
ipykernel>=6.25.0

# Testing
pytest>=7.4.0
pytest-cov>=4.1.0

# Optional: OSM (heavy; pulls networkx + requests).
# Not needed for the base pipeline — only the OSM loader.
# Install with:  pip install osmnx>=2.0
# or:            pip install -e ".[osm]"
# osmnx>=2.0

# Optional: dashboard (pulls in a web-app framework and a WebGL map layer).
# Not needed for the base pipeline — only the Streamlit dashboard.
# Install with:  pip install -e ".[dashboard]"
# streamlit>=1.38.0
# pydeck>=0.9.0
